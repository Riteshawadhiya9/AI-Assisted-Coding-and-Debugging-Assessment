# Trees & BST Debugging Problems (10 Questions: C, C++, Java)

TREE_QUESTIONS = [
    {
        "id": "q_tree_max_depth",
        "title": "Maximum Depth of Binary Tree",
        "problemStatement": "Given the root of a binary tree, return its maximum depth.\n\nA binary tree's maximum depth is the number of nodes along the longest path from the root node down to the farthest leaf node.",
        "topic": "trees",
        "subtopic": "depth-height",
        "difficulty": "easy",
        "estimatedTime": 15,
        "primaryBugType": "incorrect recursion",
        "bugConcept": "Missing +1 for current root node in recursive depth aggregation",
        "intendedApproach": "Base case: if root is null return 0. Recursively find maxDepth(left) and maxDepth(right). Return 1 + max(leftDepth, rightDepth).",
        "explanation": "Omitting `1 +` when returning the depth calculates 0 at every node, returning 0 for any non-empty tree.",
        "constraints": [
            "The number of nodes in the tree is in the range [0, 10^4].",
            "-100 <= Node.val <= 100",
        ],
        "visibleTestCases": [
            {
                "id": 1,
                "input": "root = [3,9,20,null,null,15,7]",
                "expectedOutput": "3",
                "isHidden": False,
                "explanation": "The longest path is 3 -> 20 -> 15 (or 3 -> 20 -> 7), which has 3 nodes.",
            },
            {
                "id": 2,
                "input": "root = [1,null,2]",
                "expectedOutput": "2",
                "isHidden": False,
                "explanation": "The longest path is 1 -> 2, which has 2 nodes.",
            },
        ],
        "hiddenTestCases": [
            {
                "id": 3,
                "input": "root = []",
                "expectedOutput": "0",
                "isHidden": True,
            },
            {
                "id": 4,
                "input": "root = [0]",
                "expectedOutput": "1",
                "isHidden": True,
            },
            {
                "id": 5,
                "input": "root = [1,2,3,4,null,null,5]",
                "expectedOutput": "3",
                "isHidden": True,
            },
        ],
        "expectedComplexity": {
            "time": "O(N)",
            "space": "O(H)",
        },
        "tags": [
            "trees",
            "recursion",
            "easy",
        ],
        "visualData": {
            "type": "tree",
            "title": "Binary Tree Depth Hierarchy",
            "data": [
                3,
                9,
                20,
                None,
                None,
                15,
                7,
            ],
        },
        "implementations": {
            "c": {
                "buggyCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n\nint maxDepth(struct TreeNode* root) {\n    if (root == NULL) return 0;\n    int left = maxDepth(root->left);\n    int right = maxDepth(root->right);\n    return (left > right) ? left : right;\n}",
                "correctCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n\nint maxDepth(struct TreeNode* root) {\n    if (root == NULL) return 0;\n    int left = maxDepth(root->left);\n    int right = maxDepth(root->right);\n    return 1 + ((left > right) ? left : right);\n}",
            },
            "cpp": {
                "buggyCode": "struct TreeNode { int val; TreeNode *left; TreeNode *right; };\n#include <algorithm>\nusing namespace std;\n\nint maxDepth(TreeNode* root) {\n    if (!root) return 0;\n    int left = maxDepth(root->left);\n    int right = maxDepth(root->right);\n    return max(left, right);\n}",
                "correctCode": "struct TreeNode { int val; TreeNode *left; TreeNode *right; };\n#include <algorithm>\nusing namespace std;\n\nint maxDepth(TreeNode* root) {\n    if (!root) return 0;\n    int left = maxDepth(root->left);\n    int right = maxDepth(root->right);\n    return 1 + max(left, right);\n}",
            },
            "java": {
                "buggyCode": "class TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    public int maxDepth(TreeNode root) {\n        if (root == null) return 0;\n        int left = maxDepth(root.left);\n        int right = maxDepth(root.right);\n        return Math.max(left, right);\n    }\n}",
                "correctCode": "class TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    public int maxDepth(TreeNode root) {\n        if (root == null) return 0;\n        int left = maxDepth(root.left);\n        int right = maxDepth(root.right);\n        return 1 + Math.max(left, right);\n    }\n}",
            },
        },
    },
    {
        "id": "q_tree_invert",
        "title": "Invert Binary Tree",
        "problemStatement": "Given the root of a binary tree, invert the tree, and return its root.\n\nInverting a binary tree means swapping every left and right child recursively.",
        "topic": "trees",
        "subtopic": "tree-transformation",
        "difficulty": "easy",
        "estimatedTime": 15,
        "primaryBugType": "incorrect traversal",
        "bugConcept": "Overwriting left pointer before recursively inverting right subtree, which recursively inverts the same subtree twice",
        "intendedApproach": "Store inverted left in a temporary variable or invert subtrees first before swapping root->left and root->right.",
        "explanation": "If `root->left = invertTree(root->right)` is called first, the original `root->left` reference is lost. Subsequent call `invertTree(root->left)` operates on the already-inverted right subtree.",
        "constraints": [
            "The number of nodes in the tree is in the range [0, 100].",
            "-100 <= Node.val <= 100",
        ],
        "visibleTestCases": [
            {
                "id": 1,
                "input": "root = [4,2,7,1,3,6,9]",
                "expectedOutput": "[4,7,2,9,6,3,1]",
                "isHidden": False,
                "explanation": "Every left subtree and right subtree is mirrored across the vertical axis.",
            },
            {
                "id": 2,
                "input": "root = [2,1,3]",
                "expectedOutput": "[2,3,1]",
                "isHidden": False,
                "explanation": "The left child 1 and right child 3 are swapped to become right child 1 and left child 3.",
            },
        ],
        "hiddenTestCases": [
            {
                "id": 3,
                "input": "root = []",
                "expectedOutput": "[]",
                "isHidden": True,
            },
            {
                "id": 4,
                "input": "root = [1]",
                "expectedOutput": "[1]",
                "isHidden": True,
            },
            {
                "id": 5,
                "input": "root = [1,2]",
                "expectedOutput": "[1,null,2]",
                "isHidden": True,
            },
        ],
        "expectedComplexity": {
            "time": "O(N)",
            "space": "O(H)",
        },
        "tags": [
            "trees",
            "recursion",
            "easy",
        ],
        "visualData": {
            "type": "tree",
            "title": "Mirror Symmetry Model",
            "data": [
                4,
                2,
                7,
                1,
                3,
                6,
                9,
            ],
        },
        "implementations": {
            "c": {
                "buggyCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n#include <stdlib.h>\n\nstruct TreeNode* invertTree(struct TreeNode* root) {\n    if (root == NULL) return NULL;\n    root->left = invertTree(root->right);\n    root->right = invertTree(root->left);\n    return root;\n}",
                "correctCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n#include <stdlib.h>\n\nstruct TreeNode* invertTree(struct TreeNode* root) {\n    if (root == NULL) return NULL;\n    struct TreeNode* temp = root->left;\n    root->left = invertTree(root->right);\n    root->right = invertTree(temp);\n    return root;\n}",
            },
            "cpp": {
                "buggyCode": "struct TreeNode { int val; TreeNode *left; TreeNode *right; };\n\nTreeNode* invertTree(TreeNode* root) {\n    if (!root) return nullptr;\n    root->left = invertTree(root->right);\n    root->right = invertTree(root->left);\n    return root;\n}",
                "correctCode": "struct TreeNode { int val; TreeNode *left; TreeNode *right; };\n\nTreeNode* invertTree(TreeNode* root) {\n    if (!root) return nullptr;\n    TreeNode* temp = root->left;\n    root->left = invertTree(root->right);\n    root->right = invertTree(temp);\n    return root;\n}",
            },
            "java": {
                "buggyCode": "class TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    public TreeNode invertTree(TreeNode root) {\n        if (root == null) return null;\n        root.left = invertTree(root.right);\n        root.right = invertTree(root.left);\n        return root;\n    }\n}",
                "correctCode": "class Solution {\n    public TreeNode invertTree(TreeNode root) {\n        if (root == null) return null;\n        TreeNode temp = root.left;\n        root.left = invertTree(root.right);\n        root.right = invertTree(temp);\n        return root;\n    }\n}",
            },
        },
    },
    {
        "id": "q_tree_validate_bst",
        "title": "Validate Binary Search Tree",
        "problemStatement": "Given the root of a binary tree, determine if it is a valid binary search tree (BST).\n\nA valid BST is defined as follows:\n- The left subtree of a node contains only nodes with keys strictly less than the node's key.\n- The right subtree of a node contains only nodes with keys strictly greater than the node's key.\n- Both the left and right subtrees must also be binary search trees.",
        "topic": "trees",
        "subtopic": "bst",
        "difficulty": "medium",
        "estimatedTime": 20,
        "primaryBugType": "incorrect recursion",
        "bugConcept": "Only comparing immediate child node values instead of propagating global min/max range limits",
        "intendedApproach": "Pass lower and upper boundary bounds (minVal, maxVal) through recursion. For left child, update upper bound = node.val; for right child, update lower bound = node.val.",
        "explanation": "Comparing only `node.val > node.left.val` fails to catch violations in deeper descendants (e.g., in `[5, 1, 4, null, null, 3, 6]`, 3 is in 5's right subtree but is less than 5).",
        "constraints": [
            "The number of nodes in the tree is in the range [1, 10^4].",
            "-2^31 <= Node.val <= 2^31 - 1",
        ],
        "visibleTestCases": [
            {
                "id": 1,
                "input": "root = [2,1,3]",
                "expectedOutput": "true",
                "isHidden": False,
                "explanation": "The root node's value is 2, with left child 1 (< 2) and right child 3 (> 2), which is a valid BST.",
            },
            {
                "id": 2,
                "input": "root = [5,1,4,null,null,3,6]",
                "expectedOutput": "false",
                "isHidden": False,
                "explanation": "The root node's value is 5 but its right child's value is 4, which violates the BST property.",
            },
        ],
        "hiddenTestCases": [
            {
                "id": 3,
                "input": "root = [10,5,15,null,null,6,20]",
                "expectedOutput": "false",
                "isHidden": True,
            },
            {
                "id": 4,
                "input": "root = [1,1]",
                "expectedOutput": "false",
                "isHidden": True,
            },
            {
                "id": 5,
                "input": "root = [2147483647]",
                "expectedOutput": "true",
                "isHidden": True,
            },
        ],
        "expectedComplexity": {
            "time": "O(N)",
            "space": "O(H)",
        },
        "tags": [
            "trees",
            "bst",
            "recursion",
            "medium",
        ],
        "visualData": {
            "type": "tree",
            "title": "Subtree Range Validation",
            "data": [
                5,
                1,
                4,
                None,
                None,
                3,
                6,
            ],
        },
        "implementations": {
            "c": {
                "buggyCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n#include <stdbool.h>\n#include <stdlib.h>\n\nbool isValidBST(struct TreeNode* root) {\n    if (root == NULL) return true;\n    if (root->left && root->left->val >= root->val) return false;\n    if (root->right && root->right->val <= root->val) return false;\n    return isValidBST(root->left) && isValidBST(root->right);\n}",
                "correctCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n#include <stdbool.h>\n#include <stdlib.h>\n\nbool validate(struct TreeNode* node, long minVal, long maxVal) {\n    if (node == NULL) return true;\n    if (node->val <= minVal || node->val >= maxVal) return false;\n    return validate(node->left, minVal, node->val) && validate(node->right, node->val, maxVal);\n}\n\nbool isValidBST(struct TreeNode* root) {\n    return validate(root, -2147483649LL, 2147483648LL);\n}",
            },
            "cpp": {
                "buggyCode": "struct TreeNode { int val; TreeNode *left; TreeNode *right; };\n\nbool isValidBST(TreeNode* root) {\n    if (!root) return true;\n    if (root->left && root->left->val >= root->val) return false;\n    if (root->right && root->right->val <= root->val) return false;\n    return isValidBST(root->left) && isValidBST(root->right);\n}",
                "correctCode": "struct TreeNode { int val; TreeNode *left; TreeNode *right; };\n#include <climits>\n\nbool validate(TreeNode* node, long minVal, long maxVal) {\n    if (!node) return true;\n    if (node->val <= minVal || node->val >= maxVal) return false;\n    return validate(node->left, minVal, node->val) && validate(node->right, node->val, maxVal);\n}\n\nbool isValidBST(TreeNode* root) {\n    return validate(root, LONG_MIN, LONG_MAX);\n}",
            },
            "java": {
                "buggyCode": "class TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    public boolean isValidBST(TreeNode root) {\n        if (root == null) return true;\n        if (root.left != null && root.left.val >= root.val) return false;\n        if (root.right != null && root.right.val <= root.val) return false;\n        return isValidBST(root.left) && isValidBST(root.right);\n    }\n}",
                "correctCode": "class TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    private boolean validate(TreeNode node, Long minVal, Long maxVal) {\n        if (node == null) return true;\n        if ((minVal != null && node.val <= minVal) || (maxVal != null && node.val >= maxVal)) return false;\n        return validate(node.left, minVal, (long)node.val) && validate(node.right, (long)node.val, maxVal);\n    }\n\n    public boolean isValidBST(TreeNode root) {\n        return validate(root, null, null);\n    }\n}",
            },
        },
    },
    {
        "id": "q_tree_lca_binary_tree",
        "title": "Lowest Common Ancestor of a Binary Tree",
        "problemStatement": "Given a binary tree, find the lowest common ancestor (LCA) of two given nodes in the tree.\n\nAccording to the definition of LCA on Wikipedia: \"The lowest common ancestor is defined between two nodes `p` and `q` as the lowest node in `T` that has both `p` and `q` as descendants (where we allow a node to be a descendant of itself).\"",
        "topic": "trees",
        "subtopic": "lca",
        "difficulty": "medium",
        "estimatedTime": 20,
        "primaryBugType": "incorrect recursion",
        "bugConcept": "Returning null when only one branch returns a match instead of propagating the non-null found node",
        "intendedApproach": "If root is null, p, or q, return root. Recurse left and right. If left != null && right != null return root. Else return (left != null ? left : right).",
        "explanation": "When node p is found in the left subtree and q is not found in the right subtree (because q is a child of p or in left), returning null loses the reference to p. We must return whichever branch found a match.",
        "constraints": [
            "The number of nodes in the tree is in the range [2, 10^5].",
            "-10^9 <= Node.val <= 10^9",
            "All Node.val are unique.",
            "p != q and both p and q exist in the tree.",
        ],
        "visibleTestCases": [
            {
                "id": 1,
                "input": "root = [3,5,1,6,2,0,8,null,null,7,4], p = 5, q = 1",
                "expectedOutput": "3",
                "isHidden": False,
                "explanation": "The LCA of nodes 5 and 1 is 3 since 3 is the lowest node with both 5 and 1 as descendants.",
            },
            {
                "id": 2,
                "input": "root = [3,5,1,6,2,0,8,null,null,7,4], p = 5, q = 4",
                "expectedOutput": "5",
                "isHidden": False,
                "explanation": "The LCA of nodes 5 and 4 is 5, since a node can be a descendant of itself according to the LCA definition.",
            },
        ],
        "hiddenTestCases": [
            {
                "id": 3,
                "input": "root = [1,2], p = 1, q = 2",
                "expectedOutput": "1",
                "isHidden": True,
            },
            {
                "id": 4,
                "input": "root = [2,1,3], p = 1, q = 3",
                "expectedOutput": "2",
                "isHidden": True,
            },
            {
                "id": 5,
                "input": "root = [6,2,8,0,4,7,9,null,null,3,5], p = 3, q = 5",
                "expectedOutput": "4",
                "isHidden": True,
            },
        ],
        "expectedComplexity": {
            "time": "O(N)",
            "space": "O(H)",
        },
        "tags": [
            "trees",
            "recursion",
            "lca",
            "medium",
        ],
        "visualData": {
            "type": "tree",
            "title": "LCA Search Convergence",
            "data": [
                3,
                5,
                1,
                6,
                2,
                0,
                8,
            ],
        },
        "implementations": {
            "c": {
                "buggyCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n#include <stdlib.h>\n\nstruct TreeNode* lowestCommonAncestor(struct TreeNode* root, struct TreeNode* p, struct TreeNode* q) {\n    if (root == NULL || root == p || root == q) return root;\n    struct TreeNode* left = lowestCommonAncestor(root->left, p, q);\n    struct TreeNode* right = lowestCommonAncestor(root->right, p, q);\n    if (left != NULL && right != NULL) return root;\n    return NULL;\n}",
                "correctCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n#include <stdlib.h>\n\nstruct TreeNode* lowestCommonAncestor(struct TreeNode* root, struct TreeNode* p, struct TreeNode* q) {\n    if (root == NULL || root == p || root == q) return root;\n    struct TreeNode* left = lowestCommonAncestor(root->left, p, q);\n    struct TreeNode* right = lowestCommonAncestor(root->right, p, q);\n    if (left != NULL && right != NULL) return root;\n    return (left != NULL) ? left : right;\n}",
            },
            "cpp": {
                "buggyCode": "struct TreeNode { int val; TreeNode *left; TreeNode *right; };\n\nTreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {\n    if (!root || root == p || root == q) return root;\n    TreeNode* left = lowestCommonAncestor(root->left, p, q);\n    TreeNode* right = lowestCommonAncestor(root->right, p, q);\n    if (left && right) return root;\n    return nullptr;\n}",
                "correctCode": "struct TreeNode { int val; TreeNode *left; TreeNode *right; };\n\nTreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {\n    if (!root || root == p || root == q) return root;\n    TreeNode* left = lowestCommonAncestor(root->left, p, q);\n    TreeNode* right = lowestCommonAncestor(root->right, p, q);\n    if (left && right) return root;\n    return left ? left : right;\n}",
            },
            "java": {
                "buggyCode": "class TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {\n        if (root == null || root == p || root == q) return root;\n        TreeNode left = lowestCommonAncestor(root.left, p, q);\n        TreeNode right = lowestCommonAncestor(root.right, p, q);\n        if (left != null && right != null) return root;\n        return null;\n    }\n}",
                "correctCode": "class TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {\n        if (root == null || root == p || root == q) return root;\n        TreeNode left = lowestCommonAncestor(root.left, p, q);\n        TreeNode right = lowestCommonAncestor(root.right, p, q);\n        if (left != null && right != null) return root;\n        return left != null ? left : right;\n    }\n}",
            },
        },
    },
    {
        "id": "q_tree_diameter",
        "title": "Diameter of Binary Tree",
        "problemStatement": "Given the root of a binary tree, return the length of the diameter of the tree.\n\nThe diameter of a binary tree is the length of the longest path between any two nodes in a tree. This path may or may not pass through the root.\n\nThe length of a path between two nodes is represented by the number of edges between them.",
        "topic": "trees",
        "subtopic": "depth-height",
        "difficulty": "easy",
        "estimatedTime": 15,
        "primaryBugType": "off-by-one",
        "bugConcept": "Counting nodes instead of edges (leftHeight + rightHeight + 1 instead of leftHeight + rightHeight)",
        "intendedApproach": "Compute heights recursively. At each node, diameter candidate is leftHeight + rightHeight (edges). Update global max diameter, and return 1 + max(leftHeight, rightHeight) for subtree height.",
        "explanation": "The number of edges on the longest path through a node with left height L and right height R is L + R. Adding 1 counts the total number of nodes instead of edges.",
        "constraints": [
            "The number of nodes in the tree is in the range [1, 10^4].",
            "-100 <= Node.val <= 100",
        ],
        "visibleTestCases": [
            {
                "id": 1,
                "input": "root = [1,2,3,4,5]",
                "expectedOutput": "3",
                "isHidden": False,
                "explanation": "3 is the length of the path [4,2,1,3] or [5,2,1,3], which has 3 edges.",
            },
            {
                "id": 2,
                "input": "root = [1,2]",
                "expectedOutput": "1",
                "isHidden": False,
                "explanation": "The path between 1 and 2 has 1 edge.",
            },
        ],
        "hiddenTestCases": [
            {
                "id": 3,
                "input": "root = [1]",
                "expectedOutput": "0",
                "isHidden": True,
            },
            {
                "id": 4,
                "input": "root = [1,2,3,4,5,6,7]",
                "expectedOutput": "4",
                "isHidden": True,
            },
            {
                "id": 5,
                "input": "root = [4,-7,-3,null,null,-9,-3,9,-7,-4,null,6]",
                "expectedOutput": "8",
                "isHidden": True,
            },
        ],
        "expectedComplexity": {
            "time": "O(N)",
            "space": "O(H)",
        },
        "tags": [
            "trees",
            "recursion",
            "depth-height",
            "easy",
        ],
        "visualData": {
            "type": "tree",
            "title": "Diameter Path: 4 -> 2 -> 1 -> 3",
            "data": [
                1,
                2,
                3,
                4,
                5,
            ],
        },
        "implementations": {
            "c": {
                "buggyCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n\nint height(struct TreeNode* node, int* maxDiameter) {\n    if (node == 0) return 0;\n    int l = height(node->left, maxDiameter);\n    int r = height(node->right, maxDiameter);\n    if (l + r + 1 > *maxDiameter) *maxDiameter = l + r + 1;\n    return 1 + ((l > r) ? l : r);\n}\n\nint diameterOfBinaryTree(struct TreeNode* root) {\n    int maxDiameter = 0;\n    height(root, &maxDiameter);\n    return maxDiameter;\n}",
                "correctCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n\nint height(struct TreeNode* node, int* maxDiameter) {\n    if (node == 0) return 0;\n    int l = height(node->left, maxDiameter);\n    int r = height(node->right, maxDiameter);\n    if (l + r > *maxDiameter) *maxDiameter = l + r;\n    return 1 + ((l > r) ? l : r);\n}\n\nint diameterOfBinaryTree(struct TreeNode* root) {\n    int maxDiameter = 0;\n    height(root, &maxDiameter);\n    return maxDiameter;\n}",
            },
            "cpp": {
                "buggyCode": "struct TreeNode { int val; TreeNode *left; TreeNode *right; };\n#include <algorithm>\nusing namespace std;\n\nint height(TreeNode* node, int& maxDiameter) {\n    if (!node) return 0;\n    int l = height(node->left, maxDiameter);\n    int r = height(node->right, maxDiameter);\n    maxDiameter = max(maxDiameter, l + r + 1);\n    return 1 + max(l, r);\n}\n\nint diameterOfBinaryTree(TreeNode* root) {\n    int maxDiameter = 0;\n    height(root, maxDiameter);\n    return maxDiameter;\n}",
                "correctCode": "struct TreeNode { int val; TreeNode *left; TreeNode *right; };\n#include <algorithm>\nusing namespace std;\n\nint height(TreeNode* node, int& maxDiameter) {\n    if (!node) return 0;\n    int l = height(node->left, maxDiameter);\n    int r = height(node->right, maxDiameter);\n    maxDiameter = max(maxDiameter, l + r);\n    return 1 + max(l, r);\n}\n\nint diameterOfBinaryTree(TreeNode* root) {\n    int maxDiameter = 0;\n    height(root, maxDiameter);\n    return maxDiameter;\n}",
            },
            "java": {
                "buggyCode": "class TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    private int maxDiameter = 0;\n    private int height(TreeNode node) {\n        if (node == null) return 0;\n        int l = height(node.left);\n        int r = height(node.right);\n        maxDiameter = Math.max(maxDiameter, l + r + 1);\n        return 1 + Math.max(l, r);\n    }\n    public int diameterOfBinaryTree(TreeNode root) {\n        maxDiameter = 0;\n        height(root);\n        return maxDiameter;\n    }\n}",
                "correctCode": "class TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    private int maxDiameter = 0;\n    private int height(TreeNode node) {\n        if (node == null) return 0;\n        int l = height(node.left);\n        int r = height(node.right);\n        maxDiameter = Math.max(maxDiameter, l + r);\n        return 1 + Math.max(l, r);\n    }\n    public int diameterOfBinaryTree(TreeNode root) {\n        maxDiameter = 0;\n        height(root);\n        return maxDiameter;\n    }\n}",
            },
        },
    },
    {
        "id": "q_tree_level_order_traversal",
        "title": "Binary Tree Level Order Traversal",
        "problemStatement": "Given the root of a binary tree, return the level order traversal of its nodes' values (i.e., from left to right, level by level).",
        "topic": "trees",
        "subtopic": "bfs",
        "difficulty": "medium",
        "estimatedTime": 20,
        "primaryBugType": "boundary",
        "bugConcept": "Dynamically re-evaluating queue size inside loop rather than snapshotting fixed level size, grouping multi-level nodes together",
        "intendedApproach": "Use a queue for BFS. For each level, record `levelSize = queue.size()`. Loop exactly `levelSize` times to dequeue all nodes for the current level and enqueue their children.",
        "explanation": "If the loop condition calls `i < queue.size()` without freezing level size first, newly added children dynamically increase queue size, dumping all tree nodes into a single flattened level.",
        "constraints": [
            "The number of nodes in the tree is in the range [0, 2000].",
            "-1000 <= Node.val <= 1000",
        ],
        "visibleTestCases": [
            {
                "id": 1,
                "input": "root = [3,9,20,null,null,15,7]",
                "expectedOutput": "[[3],[9,20],[15,7]]",
                "isHidden": False,
                "explanation": "Level 0 has [3], level 1 has [9, 20], and level 2 has [15, 7].",
            },
            {
                "id": 2,
                "input": "root = [1]",
                "expectedOutput": "[[1]]",
                "isHidden": False,
                "explanation": "Level 0 has node [1].",
            },
        ],
        "hiddenTestCases": [
            {
                "id": 3,
                "input": "root = []",
                "expectedOutput": "[]",
                "isHidden": True,
            },
            {
                "id": 4,
                "input": "root = [1,2,null,3,null,4]",
                "expectedOutput": "[[1],[2],[3],[4]]",
                "isHidden": True,
            },
            {
                "id": 5,
                "input": "root = [1,2,3,4,5,6,7]",
                "expectedOutput": "[[1],[2,3],[4,5,6,7]]",
                "isHidden": True,
            },
        ],
        "expectedComplexity": {
            "time": "O(N)",
            "space": "O(N)",
        },
        "tags": [
            "trees",
            "bfs",
            "queue",
            "medium",
        ],
        "visualData": {
            "type": "tree",
            "title": "Level-by-Level Tree Partition",
            "data": [
                3,
                9,
                20,
                None,
                None,
                15,
                7,
            ],
        },
        "implementations": {
            "c": {
                "buggyCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n#include <stdlib.h>\n\n// Level order traversal simulator\nvoid levelOrder(struct TreeNode* root) {\n    if (!root) return;\n    struct TreeNode* queue[2000];\n    int head = 0, tail = 0;\n    queue[tail++] = root;\n    while (head < tail) {\n        for (int i = head; i < tail; i++) {\n            struct TreeNode* node = queue[head++];\n            if (node->left) queue[tail++] = node->left;\n            if (node->right) queue[tail++] = node->right;\n        }\n    }\n}",
                "correctCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n#include <stdlib.h>\n\nvoid levelOrder(struct TreeNode* root) {\n    if (!root) return;\n    struct TreeNode* queue[2000];\n    int head = 0, tail = 0;\n    queue[tail++] = root;\n    while (head < tail) {\n        int levelSize = tail - head;\n        for (int i = 0; i < levelSize; i++) {\n            struct TreeNode* node = queue[head++];\n            if (node->left) queue[tail++] = node->left;\n            if (node->right) queue[tail++] = node->right;\n        }\n    }\n}",
            },
            "cpp": {
                "buggyCode": "#include <vector>\n#include <queue>\nusing namespace std;\n\nstruct TreeNode { int val; TreeNode *left; TreeNode *right; };\n\nvector<vector<int>> levelOrder(TreeNode* root) {\n    if (!root) return {};\n    vector<vector<int>> res;\n    queue<TreeNode*> q;\n    q.push(root);\n    while (!q.empty()) {\n        vector<int> level;\n        for (size_t i = 0; i < q.size(); i++) {\n            TreeNode* node = q.front(); q.pop();\n            level.push_back(node->val);\n            if (node->left) q.push(node->left);\n            if (node->right) q.push(node->right);\n        }\n        res.push_back(level);\n    }\n    return res;\n}",
                "correctCode": "#include <vector>\n#include <queue>\nusing namespace std;\n\nstruct TreeNode { int val; TreeNode *left; TreeNode *right; };\n\nvector<vector<int>> levelOrder(TreeNode* root) {\n    if (!root) return {};\n    vector<vector<int>> res;\n    queue<TreeNode*> q;\n    q.push(root);\n    while (!q.empty()) {\n        vector<int> level;\n        int levelSize = q.size();\n        for (int i = 0; i < levelSize; i++) {\n            TreeNode* node = q.front(); q.pop();\n            level.push_back(node->val);\n            if (node->left) q.push(node->left);\n            if (node->right) q.push(node->right);\n        }\n        res.push_back(level);\n    }\n    return res;\n}",
            },
            "java": {
                "buggyCode": "import java.util.*;\nclass TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    public List<List<Integer>> levelOrder(TreeNode root) {\n        List<List<Integer>> res = new ArrayList<>();\n        if (root == null) return res;\n        Queue<TreeNode> q = new LinkedList<>();\n        q.offer(root);\n        while (!q.isEmpty()) {\n            List<Integer> level = new ArrayList<>();\n            for (int i = 0; i < q.size(); i++) {\n                TreeNode node = q.poll();\n                level.add(node.val);\n                if (node.left != null) q.offer(node.left);\n                if (node.right != null) q.offer(node.right);\n            }\n            res.add(level);\n        }\n        return res;\n    }\n}",
                "correctCode": "import java.util.*;\nclass TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    public List<List<Integer>> levelOrder(TreeNode root) {\n        List<List<Integer>> res = new ArrayList<>();\n        if (root == null) return res;\n        Queue<TreeNode> q = new LinkedList<>();\n        q.offer(root);\n        while (!q.isEmpty()) {\n            List<Integer> level = new ArrayList<>();\n            int levelSize = q.size();\n            for (int i = 0; i < levelSize; i++) {\n                TreeNode node = q.poll();\n                level.add(node.val);\n                if (node.left != null) q.offer(node.left);\n                if (node.right != null) q.offer(node.right);\n            }\n            res.add(level);\n        }\n        return res;\n    }\n}",
            },
        },
    },
    {
        "id": "q_tree_path_sum_ii",
        "title": "Path Sum II (All Root-to-Leaf Paths)",
        "problemStatement": "Given the root of a binary tree and an integer `targetSum`, return all root-to-leaf paths where the sum of the node values in the path equals `targetSum`.\n\nEach path should be returned as a list of the node values, not node references.\n\nA root-to-leaf path is a path starting from the root and ending at any leaf node. A leaf is a node with no children.",
        "topic": "trees",
        "subtopic": "recursion",
        "difficulty": "medium",
        "estimatedTime": 20,
        "primaryBugType": "incorrect recursion",
        "bugConcept": "Failure to remove current node from path (backtrack pop) before returning to parent caller",
        "intendedApproach": "Add node.val to current path and subtract from targetSum. If node is a leaf and targetSum == 0, record a copy of the path. Recursively search left and right children. Backtrack by popping node.val from path.",
        "explanation": "If the current node is not popped from `currentPath` upon exiting the recursion frame, left branch node values bleed over into subsequent right branch traversals.",
        "constraints": [
            "The number of nodes in the tree is in the range [0, 5000].",
            "-1000 <= Node.val <= 1000",
            "-1000 <= targetSum <= 1000",
        ],
        "visibleTestCases": [
            {
                "id": 1,
                "input": "root = [5,4,8,11,null,13,4,7,2,null,null,5,1], targetSum = 22",
                "expectedOutput": "[[5,4,11,2],[5,8,4,5]]",
                "isHidden": False,
                "explanation": "There are two root-to-leaf paths that sum to 22: 5 -> 4 -> 11 -> 2 and 5 -> 8 -> 4 -> 5.",
            },
            {
                "id": 2,
                "input": "root = [1,2,3], targetSum = 5",
                "expectedOutput": "[]",
                "isHidden": False,
                "explanation": "There are no root-to-leaf paths that sum to 5.",
            },
        ],
        "hiddenTestCases": [
            {
                "id": 3,
                "input": "root = [1,2], targetSum = 0",
                "expectedOutput": "[]",
                "isHidden": True,
            },
            {
                "id": 4,
                "input": "root = [1], targetSum = 1",
                "expectedOutput": "[[1]]",
                "isHidden": True,
            },
            {
                "id": 5,
                "input": "root = [-2,null,-3], targetSum = -5",
                "expectedOutput": "[[-2,-3]]",
                "isHidden": True,
            },
        ],
        "expectedComplexity": {
            "time": "O(N^2)",
            "space": "O(N)",
        },
        "tags": [
            "trees",
            "recursion",
            "backtracking",
            "medium",
        ],
        "visualData": {
            "type": "tree",
            "title": "Root to Leaf Path Search",
            "data": [
                5,
                4,
                8,
                11,
                None,
                13,
                4,
            ],
        },
        "implementations": {
            "c": {
                "buggyCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n\nvoid findPaths(struct TreeNode* node, int target, int* path, int len) {\n    if (!node) return;\n    path[len++] = node->val;\n    if (!node->left && !node->right && node->val == target) {\n        // recorded path\n        return;\n    }\n    findPaths(node->left, target - node->val, path, len);\n    findPaths(node->right, target - node->val, path, len);\n}",
                "correctCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n\nvoid findPaths(struct TreeNode* node, int target, int* path, int len) {\n    if (!node) return;\n    path[len] = node->val;\n    if (!node->left && !node->right && node->val == target) {\n        // recorded path\n    }\n    findPaths(node->left, target - node->val, path, len + 1);\n    findPaths(node->right, target - node->val, path, len + 1);\n}",
            },
            "cpp": {
                "buggyCode": "#include <vector>\nusing namespace std;\n\nstruct TreeNode { int val; TreeNode *left; TreeNode *right; };\n\nvoid dfs(TreeNode* node, int target, vector<int>& path, vector<vector<int>>& res) {\n    if (!node) return;\n    path.push_back(node->val);\n    if (!node->left && !node->right && node->val == target) {\n        res.push_back(path);\n    }\n    dfs(node->left, target - node->val, path, res);\n    dfs(node->right, target - node->val, path, res);\n}\n\nvector<vector<int>> pathSum(TreeNode* root, int targetSum) {\n    vector<vector<int>> res;\n    vector<int> path;\n    dfs(root, targetSum, path, res);\n    return res;\n}",
                "correctCode": "#include <vector>\nusing namespace std;\n\nstruct TreeNode { int val; TreeNode *left; TreeNode *right; };\n\nvoid dfs(TreeNode* node, int target, vector<int>& path, vector<vector<int>>& res) {\n    if (!node) return;\n    path.push_back(node->val);\n    if (!node->left && !node->right && node->val == target) {\n        res.push_back(path);\n    }\n    dfs(node->left, target - node->val, path, res);\n    dfs(node->right, target - node->val, path, res);\n    path.pop_back();\n}\n\nvector<vector<int>> pathSum(TreeNode* root, int targetSum) {\n    vector<vector<int>> res;\n    vector<int> path;\n    dfs(root, targetSum, path, res);\n    return res;\n}",
            },
            "java": {
                "buggyCode": "import java.util.*;\nclass TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    private void dfs(TreeNode node, int target, List<Integer> path, List<List<Integer>> res) {\n        if (node == null) return;\n        path.add(node.val);\n        if (node.left == null && node.right == null && node.val == target) {\n            res.add(new ArrayList<>(path));\n        }\n        dfs(node.left, target - node.val, path, res);\n        dfs(node.right, target - node.val, path, res);\n    }\n\n    public List<List<Integer>> pathSum(TreeNode root, int targetSum) {\n        List<List<Integer>> res = new ArrayList<>();\n        dfs(root, targetSum, new ArrayList<>(), res);\n        return res;\n    }\n}",
                "correctCode": "import java.util.*;\nclass TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    private void dfs(TreeNode node, int target, List<Integer> path, List<List<Integer>> res) {\n        if (node == null) return;\n        path.add(node.val);\n        if (node.left == null && node.right == null && node.val == target) {\n            res.add(new ArrayList<>(path));\n        }\n        dfs(node.left, target - node.val, path, res);\n        dfs(node.right, target - node.val, path, res);\n        path.remove(path.size() - 1);\n    }\n\n    public List<List<Integer>> pathSum(TreeNode root, int targetSum) {\n        List<List<Integer>> res = new ArrayList<>();\n        dfs(root, targetSum, new ArrayList<>(), res);\n        return res;\n    }\n}",
            },
        },
    },
    {
        "id": "q_tree_kth_smallest_bst",
        "title": "Kth Smallest Element in a BST",
        "problemStatement": "Given the root of a binary search tree (BST) and an integer `k`, return the `k-th` smallest value (1-indexed) of all the values of the nodes in the tree.",
        "topic": "trees",
        "subtopic": "bst",
        "difficulty": "medium",
        "estimatedTime": 20,
        "primaryBugType": "incorrect traversal",
        "bugConcept": "Performing Pre-order traversal instead of In-order traversal to locate the kth smallest BST key",
        "intendedApproach": "Perform in-order traversal (Left -> Node -> Right). Decrement k at each visited node. When k == 0, record the answer and stop.",
        "explanation": "In-order traversal visits nodes in strictly ascending sorted order. Visiting nodes in pre-order (Node -> Left -> Right) does not produce sorted sequence, yielding the wrong kth element.",
        "constraints": [
            "The number of nodes in the tree is n.",
            "1 <= k <= n <= 10^4",
            "0 <= Node.val <= 10^4",
        ],
        "visibleTestCases": [
            {
                "id": 1,
                "input": "root = [3,1,4,null,2], k = 1",
                "expectedOutput": "1",
                "isHidden": False,
                "explanation": "The in-order sorted values are [1, 2, 3, 4]. The 1st smallest element is 1.",
            },
            {
                "id": 2,
                "input": "root = [5,3,6,2,4,null,null,1], k = 3",
                "expectedOutput": "3",
                "isHidden": False,
                "explanation": "The in-order sorted values are [1, 2, 3, 4, 5, 6]. The 3rd smallest element is 3.",
            },
        ],
        "hiddenTestCases": [
            {
                "id": 3,
                "input": "root = [1], k = 1",
                "expectedOutput": "1",
                "isHidden": True,
            },
            {
                "id": 4,
                "input": "root = [4,2,6,1,3,5,7], k = 4",
                "expectedOutput": "4",
                "isHidden": True,
            },
            {
                "id": 5,
                "input": "root = [4,2,6,1,3,5,7], k = 7",
                "expectedOutput": "7",
                "isHidden": True,
            },
        ],
        "expectedComplexity": {
            "time": "O(H + k)",
            "space": "O(H)",
        },
        "tags": [
            "trees",
            "bst",
            "in-order",
            "medium",
        ],
        "visualData": {
            "type": "tree",
            "title": "In-Order BST Traversal",
            "data": [
                3,
                1,
                4,
                None,
                2,
            ],
        },
        "implementations": {
            "c": {
                "buggyCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n\nvoid inorder(struct TreeNode* node, int* k, int* res) {\n    if (!node || *k <= 0) return;\n    (*k)--;\n    if (*k == 0) { *res = node->val; return; }\n    inorder(node->left, k, res);\n    inorder(node->right, k, res);\n}\n\nint kthSmallest(struct TreeNode* root, int k) {\n    int res = -1;\n    inorder(root, &k, &res);\n    return res;\n}",
                "correctCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n\nvoid inorder(struct TreeNode* node, int* k, int* res) {\n    if (!node || *k <= 0) return;\n    inorder(node->left, k, res);\n    (*k)--;\n    if (*k == 0) { *res = node->val; return; }\n    inorder(node->right, k, res);\n}\n\nint kthSmallest(struct TreeNode* root, int k) {\n    int res = -1;\n    inorder(root, &k, &res);\n    return res;\n}",
            },
            "cpp": {
                "buggyCode": "struct TreeNode { int val; TreeNode *left; TreeNode *right; };\n\nvoid inorder(TreeNode* node, int& k, int& res) {\n    if (!node || k <= 0) return;\n    k--;\n    if (k == 0) { res = node->val; return; }\n    inorder(node->left, k, res);\n    inorder(node->right, k, res);\n}\n\nint kthSmallest(TreeNode* root, int k) {\n    int res = -1;\n    inorder(root, k, res);\n    return res;\n}",
                "correctCode": "struct TreeNode { int val; TreeNode *left; TreeNode *right; };\n\nvoid inorder(TreeNode* node, int& k, int& res) {\n    if (!node || k <= 0) return;\n    inorder(node->left, k, res);\n    k--;\n    if (k == 0) { res = node->val; return; }\n    inorder(node->right, k, res);\n}\n\nint kthSmallest(TreeNode* root, int k) {\n    int res = -1;\n    inorder(root, k, res);\n    return res;\n}",
            },
            "java": {
                "buggyCode": "class TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    private int count, ans;\n    private void inorder(TreeNode node) {\n        if (node == null || count <= 0) return;\n        count--;\n        if (count == 0) { ans = node.val; return; }\n        inorder(node.left);\n        inorder(node.right);\n    }\n    public int kthSmallest(TreeNode root, int k) {\n        count = k;\n        ans = -1;\n        inorder(root);\n        return ans;\n    }\n}",
                "correctCode": "class TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    private int count, ans;\n    private void inorder(TreeNode node) {\n        if (node == null || count <= 0) return;\n        inorder(node.left);\n        count--;\n        if (count == 0) { ans = node.val; return; }\n        inorder(node.right);\n    }\n    public int kthSmallest(TreeNode root, int k) {\n        count = k;\n        ans = -1;\n        inorder(root);\n        return ans;\n    }\n}",
            },
        },
    },
    {
        "id": "q_tree_symmetric_tree",
        "title": "Symmetric Tree (Mirror Reflection)",
        "problemStatement": "Given the root of a binary tree, check whether it is a mirror of itself (i.e., symmetric around its center).",
        "topic": "trees",
        "subtopic": "recursion",
        "difficulty": "easy",
        "estimatedTime": 15,
        "primaryBugType": "incorrect recursion",
        "bugConcept": "Comparing left-with-left and right-with-right instead of cross-mirror left-with-right and right-with-left",
        "intendedApproach": "Helper `isMirror(t1, t2)`. If both null return true. If one is null or t1.val != t2.val return false. Recursively check `isMirror(t1.left, t2.right)` AND `isMirror(t1.right, t2.left)`.",
        "explanation": "Checking `isMirror(t1.left, t2.left)` checks if trees are identical clones rather than mirror reflections across the vertical axis.",
        "constraints": [
            "The number of nodes in the tree is in the range [1, 1000].",
            "-100 <= Node.val <= 100",
        ],
        "visibleTestCases": [
            {
                "id": 1,
                "input": "root = [1,2,2,3,4,4,3]",
                "expectedOutput": "true",
                "isHidden": False,
                "explanation": "The left subtree [2, 3, 4] is a mirror reflection of the right subtree [2, 4, 3].",
            },
            {
                "id": 2,
                "input": "root = [1,2,2,null,3,null,3]",
                "expectedOutput": "false",
                "isHidden": False,
                "explanation": "The left subtree [2, null, 3] and right subtree [2, null, 3] are identical in shape, not mirror reflections.",
            },
        ],
        "hiddenTestCases": [
            {
                "id": 3,
                "input": "root = [1]",
                "expectedOutput": "true",
                "isHidden": True,
            },
            {
                "id": 4,
                "input": "root = [1,2,2,2,null,2]",
                "expectedOutput": "false",
                "isHidden": True,
            },
            {
                "id": 5,
                "input": "root = [1,2,3]",
                "expectedOutput": "false",
                "isHidden": True,
            },
        ],
        "expectedComplexity": {
            "time": "O(N)",
            "space": "O(H)",
        },
        "tags": [
            "trees",
            "recursion",
            "easy",
        ],
        "visualData": {
            "type": "tree",
            "title": "Symmetric Mirror Reflection",
            "data": [
                1,
                2,
                2,
                3,
                4,
                4,
                3,
            ],
        },
        "implementations": {
            "c": {
                "buggyCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n#include <stdbool.h>\n#include <stdlib.h>\n\nbool isMirror(struct TreeNode* t1, struct TreeNode* t2) {\n    if (!t1 && !t2) return true;\n    if (!t1 || !t2 || t1->val != t2->val) return false;\n    return isMirror(t1->left, t2->left) && isMirror(t1->right, t2->right);\n}\n\nbool isSymmetric(struct TreeNode* root) {\n    if (!root) return true;\n    return isMirror(root->left, root->right);\n}",
                "correctCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n#include <stdbool.h>\n#include <stdlib.h>\n\nbool isMirror(struct TreeNode* t1, struct TreeNode* t2) {\n    if (!t1 && !t2) return true;\n    if (!t1 || !t2 || t1->val != t2->val) return false;\n    return isMirror(t1->left, t2->right) && isMirror(t1->right, t2->left);\n}\n\nbool isSymmetric(struct TreeNode* root) {\n    if (!root) return true;\n    return isMirror(root->left, root->right);\n}",
            },
            "cpp": {
                "buggyCode": "struct TreeNode { int val; TreeNode *left; TreeNode *right; };\n\nbool isMirror(TreeNode* t1, TreeNode* t2) {\n    if (!t1 && !t2) return true;\n    if (!t1 || !t2 || t1->val != t2->val) return false;\n    return isMirror(t1->left, t2->left) && isMirror(t1->right, t2->right);\n}\n\nbool isSymmetric(TreeNode* root) {\n    return !root || isMirror(root->left, root->right);\n}",
                "correctCode": "struct TreeNode { int val; TreeNode *left; TreeNode *right; };\n\nbool isMirror(TreeNode* t1, TreeNode* t2) {\n    if (!t1 && !t2) return true;\n    if (!t1 || !t2 || t1->val != t2->val) return false;\n    return isMirror(t1->left, t2->right) && isMirror(t1->right, t2->left);\n}\n\nbool isSymmetric(TreeNode* root) {\n    return !root || isMirror(root->left, root->right);\n}",
            },
            "java": {
                "buggyCode": "class TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    private boolean isMirror(TreeNode t1, TreeNode t2) {\n        if (t1 == null && t2 == null) return true;\n        if (t1 == null || t2 == null || t1.val != t2.val) return false;\n        return isMirror(t1.left, t2.left) && isMirror(t1.right, t2.right);\n    }\n    public boolean isSymmetric(TreeNode root) {\n        return root == null || isMirror(root.left, root.right);\n    }\n}",
                "correctCode": "class TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    private boolean isMirror(TreeNode t1, TreeNode t2) {\n        if (t1 == null && t2 == null) return true;\n        if (t1 == null || t2 == null || t1.val != t2.val) return false;\n        return isMirror(t1.left, t2.right) && isMirror(t1.right, t2.left);\n    }\n    public boolean isSymmetric(TreeNode root) {\n        return root == null || isMirror(root.left, root.right);\n    }\n}",
            },
        },
    },
    {
        "id": "q_tree_max_path_sum",
        "title": "Binary Tree Maximum Path Sum",
        "problemStatement": "A path in a binary tree is a sequence of nodes where each pair of adjacent nodes in the sequence has an edge connecting them. A node can only appear in the sequence at most once. Note that the path does not need to pass through the root.\n\nThe path sum of a path is the sum of the node's values in the path.\n\nGiven the root of a binary tree, return the maximum path sum of any non-empty path.",
        "topic": "trees",
        "subtopic": "recursion",
        "difficulty": "hard",
        "estimatedTime": 25,
        "primaryBugType": "boundary",
        "bugConcept": "Allowing negative branch sums to reduce the maximum path sum instead of clamping branch gain to 0 (max(0, branchSum))",
        "intendedApproach": "At each node, compute max branch gain from left and right clamped to >= 0: `leftGain = max(0, maxGain(left))`, `rightGain = max(0, maxGain(right))`. Update `maxSum = max(maxSum, node.val + leftGain + rightGain)` and return `node.val + max(leftGain, rightGain)`.",
        "explanation": "If a subtree yields a negative sum (e.g. -10), including it harms the path sum. Failing to clamp branch gains to 0 forces negative sums to be added.",
        "constraints": [
            "The number of nodes in the tree is in the range [1, 3 * 10^4].",
            "-1000 <= Node.val <= 1000",
        ],
        "visibleTestCases": [
            {
                "id": 1,
                "input": "root = [1,2,3]",
                "expectedOutput": "6",
                "isHidden": False,
                "explanation": "The optimal path is 2 -> 1 -> 3 with a path sum of 2 + 1 + 3 = 6.",
            },
            {
                "id": 2,
                "input": "root = [-10,9,20,null,null,15,7]",
                "expectedOutput": "42",
                "isHidden": False,
                "explanation": "The optimal path is 15 -> 20 -> 7 with a path sum of 15 + 20 + 7 = 42.",
            },
        ],
        "hiddenTestCases": [
            {
                "id": 3,
                "input": "root = [-3]",
                "expectedOutput": "-3",
                "isHidden": True,
            },
            {
                "id": 4,
                "input": "root = [2,-1]",
                "expectedOutput": "2",
                "isHidden": True,
            },
            {
                "id": 5,
                "input": "root = [-2,-1]",
                "expectedOutput": "-1",
                "isHidden": True,
            },
        ],
        "expectedComplexity": {
            "time": "O(N)",
            "space": "O(H)",
        },
        "tags": [
            "trees",
            "recursion",
            "dp",
            "hard",
        ],
        "visualData": {
            "type": "tree",
            "title": "Max Path: 15 + 20 + 7 = 42",
            "data": [
                -10,
                9,
                20,
                None,
                None,
                15,
                7,
            ],
        },
        "implementations": {
            "c": {
                "buggyCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n#include <limits.h>\n\nint maxGain(struct TreeNode* node, int* maxSum) {\n    if (!node) return 0;\n    int left = maxGain(node->left, maxSum);\n    int right = maxGain(node->right, maxSum);\n    int pathSum = node->val + left + right;\n    if (pathSum > *maxSum) *maxSum = pathSum;\n    return node->val + ((left > right) ? left : right);\n}\n\nint maxPathSum(struct TreeNode* root) {\n    int maxSum = INT_MIN;\n    maxGain(root, &maxSum);\n    return maxSum;\n}",
                "correctCode": "struct TreeNode { int val; struct TreeNode *left; struct TreeNode *right; };\n#include <limits.h>\n\nint maxGain(struct TreeNode* node, int* maxSum) {\n    if (!node) return 0;\n    int left = maxGain(node->left, maxSum);\n    int right = maxGain(node->right, maxSum);\n    if (left < 0) left = 0;\n    if (right < 0) right = 0;\n    int pathSum = node->val + left + right;\n    if (pathSum > *maxSum) *maxSum = pathSum;\n    return node->val + ((left > right) ? left : right);\n}\n\nint maxPathSum(struct TreeNode* root) {\n    int maxSum = INT_MIN;\n    maxGain(root, &maxSum);\n    return maxSum;\n}",
            },
            "cpp": {
                "buggyCode": "struct TreeNode { int val; TreeNode *left; TreeNode *right; };\n#include <algorithm>\n#include <climits>\nusing namespace std;\n\nint maxGain(TreeNode* node, int& maxSum) {\n    if (!node) return 0;\n    int left = maxGain(node->left, maxSum);\n    int right = maxGain(node->right, maxSum);\n    maxSum = max(maxSum, node->val + left + right);\n    return node->val + max(left, right);\n}\n\nint maxPathSum(TreeNode* root) {\n    int maxSum = INT_MIN;\n    maxGain(root, maxSum);\n    return maxSum;\n}",
                "correctCode": "struct TreeNode { int val; TreeNode *left; TreeNode *right; };\n#include <algorithm>\n#include <climits>\nusing namespace std;\n\nint maxGain(TreeNode* node, int& maxSum) {\n    if (!node) return 0;\n    int left = max(0, maxGain(node->left, maxSum));\n    int right = max(0, maxGain(node->right, maxSum));\n    maxSum = max(maxSum, node->val + left + right);\n    return node->val + max(left, right);\n}\n\nint maxPathSum(TreeNode* root) {\n    int maxSum = INT_MIN;\n    maxGain(root, maxSum);\n    return maxSum;\n}",
            },
            "java": {
                "buggyCode": "class TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    private int maxSum = Integer.MIN_VALUE;\n    private int maxGain(TreeNode node) {\n        if (node == null) return 0;\n        int left = maxGain(node.left);\n        int right = maxGain(node.right);\n        maxSum = Math.max(maxSum, node.val + left + right);\n        return node.val + Math.max(left, right);\n    }\n    public int maxPathSum(TreeNode root) {\n        maxSum = Integer.MIN_VALUE;\n        maxGain(root);\n        return maxSum;\n    }\n}",
                "correctCode": "class TreeNode { int val; TreeNode left; TreeNode right; }\n\nclass Solution {\n    private int maxSum = Integer.MIN_VALUE;\n    private int maxGain(TreeNode node) {\n        if (node == null) return 0;\n        int left = Math.max(0, maxGain(node.left));\n        int right = Math.max(0, maxGain(node.right));\n        maxSum = Math.max(maxSum, node.val + left + right);\n        return node.val + Math.max(left, right);\n    }\n    public int maxPathSum(TreeNode root) {\n        maxSum = Integer.MIN_VALUE;\n        maxGain(root);\n        return maxSum;\n    }\n}",
            },
        },
    },
]
